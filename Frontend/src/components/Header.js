import Logo from "../assets/Logo.webp";

function Header() {
  return (
    <header className="app-header">
      <img height={400} width={400} src={Logo} alt="'" />
    </header>
  );
}

export default Header;
